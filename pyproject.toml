[project]
name = "cloudflare-ddns"
version = "0.1.0"
description = "A DDNS script that syncs the current global IP to Cloudflare DNS A/AAAA records"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "cloudflare>=4.3.1",
    "requests>=2.32.5",
]
